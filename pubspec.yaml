name: citiisgo_mob
description: CitiisGo - Aplikasi Wisata Tasikmalaya
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.5
  dio: ^5.4.0
  url_launcher: ^6.2.5
  cached_network_image: ^3.3.1
  intl: ^0.19.0
  shared_preferences: ^2.2.2
  flutter_rating_bar: ^4.0.1
  smooth_page_indicator: ^1.1.0
  shimmer: ^3.0.0
  google_fonts: ^6.1.0
  flutter_secure_storage: ^10.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
